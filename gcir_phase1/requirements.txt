# Phase 1 (Week 1): ETL, jurisdictions, schema
geopandas>=0.14
shapely>=2.0
pyogrio>=0.7          # fast GeoDataFrame I/O, incl. KML via GDAL's libkml driver
pymongo>=4.6

# Full stack (later phases, listed here so `pip install -r` covers the PRD stack)
flask>=3.0
flask-login>=0.6
scikit-learn>=1.4     # DBSCAN, TF-IDF (§6)
python-dotenv>=1.0

# Testing
pytest>=8.0
